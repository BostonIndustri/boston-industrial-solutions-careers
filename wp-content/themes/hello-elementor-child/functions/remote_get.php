<?php
if ( ! defined( 'ABSPATH' ) ) exit; // Exit if accessed directly.

/**
 * If the function, 'hello_elem_child_get_remote_data_callback' doesn't exist.
 */
// if ( ! function_exists( 'hello_elem_child_get_remote_data_callback' ) ) {
	/**
	 * Get remote data.
	 *
	 * @param string $uri Locale stylesheet URI.
	 * @return string
	 * @since 1.0.0
	 */
// 	function hello_elem_child_get_remote_data_callback( $endpoint_url ) {
// 		if ( empty( $uri ) && is_rtl() && file_exists( get_template_directory() . '/rtl.css' ) ) {
// 			$uri = get_template_directory_uri() . '/rtl.css';
// 		}

// 		return $uri;
// 	}
// }
